[project]
name = "brand-instagram-assistant"
version = "0.1.0"
description = "Local-first Instagram comment and DM assistant for jiiwon.studio"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116,<1",
  "instagrapi>=2.2,<3",
  "uvicorn[standard]>=0.35,<1",
]

[dependency-groups]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.4,<9",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
